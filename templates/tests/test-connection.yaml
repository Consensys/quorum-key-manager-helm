apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quorum-key-manager.fullname" . }}-test-connection"
  labels:
    {{- include "quorum-key-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quorum-key-manager.fullname" . }}:{{ .Values.service.http.port }}']
  restartPolicy: Never
